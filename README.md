Simple ToDo List Smart-Contract

Requirements:
* Truffle has to be installed globaly

Usage:
1. Clone repository
1. Run `npm install` to install dependencies

Run tests:
1. `npm run local-chain`
1. `truffle test`
